#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a;
    long long b;
    long long mul;
    scanf("%d %lld",&a, &b);
    mul = a * b;
    printf("%lld",mul);
    return 0;
}
